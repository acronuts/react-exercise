import React from 'react';
import { FooterContainer } from './styled';

const Footer = () => {
  return (
    <FooterContainer>
      <div>footer text</div>
    </FooterContainer>
  );
};

export default Footer;
