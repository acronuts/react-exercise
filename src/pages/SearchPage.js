import React, { Fragment } from 'react';
import Header from '../components/Header';

const SearchPage = () => {
  return (
    <Fragment>
        <Header />
      <div>items here</div>
    </Fragment>
  );
};

export default SearchPage;
